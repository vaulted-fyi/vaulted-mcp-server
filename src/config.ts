// Minimal stub — will be replaced by Story 1.3 (CLI configuration parsing)
export const config = {
  baseUrl: process.env.VAULTED_BASE_URL ?? "https://vaulted.fyi",
  allowedDirs: [] as string[],
};
